"""Pulumi agent infrastructure — generated by SuperRobot."""

import pulumi
import pulumi_datarobot as datarobot

{% for key in runtime_param_keys %}
{{ key | lower }}_param = datarobot.RuntimeParameter(
    "{{ key }}",
    value=pulumi.Config().require("{{ key }}"),
)
{% endfor %}

agent = datarobot.CustomApplication(
    "{{ agent_name }}",
    runtime_parameters=[
{% for key in runtime_param_keys %}
        {{ key | lower }}_param,
{% endfor %}
    ],
)
