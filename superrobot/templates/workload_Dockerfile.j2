FROM python:3.11-slim

WORKDIR /app
COPY agent/agent /app
RUN pip install --no-cache-dir {{ dependencies | join(" ") }}

ENV PYTHONUNBUFFERED=1
ENV PORT=8000
EXPOSE 8000
CMD ["python", "-m", "workload_service"]
