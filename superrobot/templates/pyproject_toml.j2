[project]
name = "{{ agent_name }}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
{% for dep in dependencies %}
    "{{ dep }}",
{% endfor %}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
